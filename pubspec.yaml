name: image_collage
description: A collage image rendering for posts (Like Facebook)
version: 0.0.3
homepage: https://github.com/iliyass-zamouri/image_collage

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

